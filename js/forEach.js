//ForEach Syntax:forEach((value,index,array)=>{}) used for loop the array.

let arrays=[1,2,3,4,5,6,7,8,9,0];

arrays.forEach((value,index,array)=>{
	console.log(`The INDEX is ${value} The INDEX is ${index} The array is ${array}`);
})

